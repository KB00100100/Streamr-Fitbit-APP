<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fs.fitbitstreamr">

    <application
        android:name=".FlyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.wallet.WalletDetailActivity"></activity>
        <activity android:name=".ui.wallet.CreateWalletActivity" />
        <activity android:name=".ui.data.DataActivity" />
        <activity android:name=".ui.personal.PersonalActivity" />
        <activity android:name=".ui.setting.SettingActivity" />
        <activity android:name=".ui.connect.ConnectActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".test.TestActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.main.MainActivity" />

        <meta-data
            android:name="com.fs.fitbitstreamr.CLIENT_ID"
            android:value="22D5WF" />
        <meta-data
            android:name="com.fs.fitbitstreamr.REDIRECT_URL"
            android:value="https://www.streamr.com/" />

        <service
            android:name=".service.DeService"
            android:enabled="true"
            android:exported="true" />
    </application>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>